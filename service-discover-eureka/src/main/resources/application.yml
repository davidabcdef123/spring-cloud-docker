server:
  port: 8761                    # 指定该Eureka实例的端口
eureka:
  client:
    registerWithEureka: false #是否注册到eureka，默认true
    fetchRegistry: false #是否从eureka获取信息,默认true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
      #多连接用，分离
